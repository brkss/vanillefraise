query PlanDetails($id: String!) {
  planDetails(id: $id) {
    id
    name
    created_at
    active
    image
    description
    public
    trackedElements {
      id
      quantity
      nutriton {
        id
        code
        name
        unit
      }
    }
  }
}
