mutation CreateActivity($category: String!, $duration: String!, $feedback: String!, $calories: Float!){
  createActivity(data: {category: $category, duration: $duration, feedback: $feedback, calories: $calories}){
    status
    message
  }
}
